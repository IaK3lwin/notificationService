# Controller

The controller belongs to the interface adapter layer, but 
i chose to leave it in the infra layer. Because will use 
framework, imagine that when see you controller folder, you
are in the interface adapter layer :3

